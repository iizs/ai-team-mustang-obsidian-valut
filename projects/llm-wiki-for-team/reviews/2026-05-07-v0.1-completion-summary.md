# v0.1 완료 통합 요약

- **일자**: 2026-05-05 ~ 2026-05-07
- **주체**: Breda (Developer), Hawkeye (Evaluator)
- **대상**: SPEC v0.1 → v0.7, sheska 구현 (commits `4653899` → `52b65f0`)
- **상태**: 모든 Phase + 핫픽스 PASS, Blocking 없음

> 원본은 Discord에서 휘발됐고, 이 파일은 회고용 통합 요약이다. 자세한 결정 근거는 [decisions/](../decisions/) 참조.

## SPEC 리뷰 (v0.1 → v0.7)

### Breda 1차 검토 (SPEC v0.1)

이슈 5건 제기 (R1~R5):
- R1 Git bare repo — 서빙 방식 불명확 → **normal repo 채택** ([ADR-0001](../decisions/0001-normal-vs-bare-repo.md))
- R2 `_sources/` 바이너리 git 추적 → **git 비추적, Docker Volume/Local Path 분리** ([ADR-0002](../decisions/0002-source-store-not-tracked.md))
- R3 Edit "관련 페이지 찾기" 미정 → 2단계 → 단일 페이지 컨텍스트로 단순화 ([ADR-0009](../decisions/0009-edit-single-page.md))
- R4 Lint 비용 폭발 → **v0.2+로 보류** ([ADR-0005](../decisions/0005-lint-deferred.md))
- R5 MVP 범위 과대 → DOCX/URL 제외 ([ADR-0003](../decisions/0003-mvp-file-formats.md))

### Hawkeye SC 작성 (SPEC v0.3)

SC-1~SC-30 작성. Blocking 22개 / Advisory 8개. 핵심 판단:
- SC-13 `[[페이지명]]` 형식 → Blocking (Obsidian 호환의 핵심)
- SC-14 `_sheska.yaml` + frontmatter `sources` → Blocking (로컬 sync 선제 조건)
- SC-26 zip/git에 `_sheska.yaml` 포함 → Blocking

### Breda 2차 검토 (SPEC v0.6)

이슈 6건 (O1, O2, R1~R4):
- O1 SC-9 Blocking인데 v0.2+ 보류와 충돌 → **Advisory/v0.2+로 변경**
- O2 MVP 범위 번호 오류 → typo 수정
- R1 EDIT payload에 `page_path` 누락 → `{edit_text, page_path}`로 확정
- R2 git server 컴포넌트 미정 → **nginx + `git http-backend`**
- R3 index.md LLM 갱신 비용 → Known Limitation 명시
- R4 백그라운드 워커 구현 미정 → **FastAPI BackgroundTasks** ([ADR-0006](../decisions/0006-fastapi-backgroundtasks.md))

### Hawkeye SC 보강 (SPEC v0.7)

SC-9 변경, SC-11 분리(즉시 응답 / Ingest 완료), SC-16-b/c 추가(index.md 갱신, log.md 형식).

## Phase 검증

### Phase 1 (인증, Job Queue) — Breda commit `4653899`

Hawkeye 검증: 7개 SC, **PASS**. Blocking 없음.

| SC | 결과 |
|----|------|
| SC-1~3 | PASS |
| SC-4 | PARTIAL (원본 투입 차단은 Phase 2에서 완성) |
| SC-5 | PARTIAL (JWT 무효화 미구현, Advisory) |
| SC-17-a/b | PASS |

### Phase 2 (Source Ingest, LLM Pipeline) — Breda commit `3cc3342`

Hawkeye 검증: 14개 SC, **Blocking 1건 발견**.

- SC-14 PARTIAL → `ensure_sheska_yaml()` 호출 안 됨, `source_base_url` config 누락
- 수정 commit `575238c`로 해소 → SC-14, SC-26 모두 PASS

### Phase 3 (Frontend, nginx, zip) — Breda commit `e04f66c`

Hawkeye 검증: 9개 SC, **PASS**. Blocking 없음.

(SC-15 Edit 입력창, SC-17-c, SC-18~22, SC-24~25, SC-27~28)

## 실사용 핫픽스 (Phase 3 종료 후)

Kirin 실사용 중 발견된 이슈와 수정:

| # | 증상 | 원인 | 수정 commit |
|---|------|------|-------------|
| 1 | "다짜고짜 로그인인데 계정 없음" | 첫 Admin 시딩 경로 미정의 | `cdbe55c` (`seed_admin.py` 추가) |
| 2 | 로그인 후 다시 로그인 페이지로 떨어짐 | `GET /api/users/me` 엔드포인트 누락 | `01e701b` |
| 3 | LLM이 Anthropic API 키 요구 (Ollama 설정인데도) | `.env`를 backend cwd 기준으로 찾음 | `6576c10` (프로젝트 루트도 탐색) |
| 4 | Ingest SUCCESS인데 위키 미생성 | LLM 출력 파싱 실패 시 silent return | `d9989e0` (명시적 ValueError) |
| 5 | LLM이 평론형 응답 ("This is a recipe...") | system prompt instruction following 약함 | `52427d1` (프롬프트 강화) + `93c17d4` (system→user prepend) |
| 6 | LLM이 `---` + 본문만 줌 (yaml 누락) | 파서가 정상 frontmatter만 인식 | `86ad5f9` (4단계 fallback + 자동 frontmatter) |

각 핫픽스는 Hawkeye가 회귀/원칙 부합성 검증 완료. 평론형 응답만 명시적 FAILED 처리 원칙 유지 (silent SUCCESS 금지).

## v0.1 최종 상태

- 코드: https://github.com/iizs/sheska — `52b65f0`
- 테스트: 41/41 통과 (mock 기반)
- SC: SC-1~SC-30 중 Blocking 22개 모두 PASS, Advisory 8개 중 SC-5/SC-20/SC-29/SC-30은 v0.2+ 검토
- 미해결 Blocking: 없음

## 후속 작업

- 문서 구조 분리 (단일 SPEC.md → 본 vault 구조) — 본 회고 작성 시점에 완료
- v0.2 백로그: [backlog.md](../backlog.md)
