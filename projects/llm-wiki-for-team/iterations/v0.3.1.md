# v0.3.1 — Phase B: Plan 액션 실행 + Wiki Command

## 기간 / 상태

2026-05-09 ~ ? / **Planning** (요건 검토 중)

## 목표

[ADR-0011 Phase A](../decisions/0011-staged-llm-pipeline.md)에서 skip + log였던 `merge_into`/`supersede`를 정상 실행으로 전환하고, 사용자가 자연어로 다중 페이지 작업(분리/통합/삭제)을 요청할 수 있는 **Wiki Command** 진입점을 도입한다. 페이지 삭제 라이프사이클도 함께 도입.

## 스코프

**포함:**
- [ADR-0012 Phase B](../decisions/0012-phase-b-actions-and-wiki-command.md) 전체:
  - Plan 액션 `merge_into`/`supersede` 정상 실행
  - `create` 충돌 자동 강등의 Phase B 정상 실행
  - 신규 Plan 액션 `delete` (페이지 삭제)
  - 신규 Job 타입 `WIKI_COMMAND` + 자연어 명령 처리
  - Wiki Command UI (별도 탭/페이지)
  - log.md `executed:` 라인 형식 추가
  - 단일 job + 다중 액션 구조 유지 (현재 Phase A 구조 그대로)

**제외 (v0.4+):**
- 사용자 승인 절차 (Plan Review 화면) — 웹소켓/이슈트래킹 수준 복잡도
- redirect 기능 — 데이터 모델 단순 유지
- Sub-job 모델 — 운영 필요성 발생 시
- Lint를 통한 삭제 정리 — B-6 (별도)

## 태스크 (셋이 검토 후 확정)

| ID | 제목 | 담당 | 추정 |
|----|------|------|------|
| T-3.1-0 | ADR-0012 + 신규 SC 작성/검토 | Hawkeye | TBD |
| T-3.1-1 | `DeleteAction` Pydantic 모델 + Plan 액션 분기 | Breda | TBD |
| T-3.1-2 | `merge_into` 실행 (read → LLM 통합 → commit + log "executed") | Breda | TBD |
| T-3.1-3 | `supersede` 실행 (read → LLM 갈아엎기 → commit + log "executed: reason") | Breda | TBD |
| T-3.1-4 | `delete` 실행 (파일 삭제 + commit + log "executed: reason") | Breda | TBD |
| T-3.1-5 | `create` 충돌 자동 강등의 정상 실행 (Phase A skip 제거) | Breda | TBD |
| T-3.1-6 | 신규 Job 타입 `WIKI_COMMAND` + `POST /api/wiki/commands` 엔드포인트 | Breda | TBD |
| T-3.1-7 | `prompts/wiki_command.txt` 작성 (자연어 → Plan 가이드, 예시 포함) | Breda | TBD |
| T-3.1-8 | Wiki Command UI (`/command` 페이지 + nav 진입점) | Breda | TBD |
| T-3.1-9 | 통합 검증 + 회귀 테스트 | Hawkeye | TBD |

## 변경/결정 (예정)

ADR-0012 채택 사항:
- Plan 액션 `merge_into`/`supersede` 실행 활성화 + 신규 `delete`
- `WIKI_COMMAND` Job 타입 추가
- 단일 job + 다중 액션 구조 유지
- 사용자 승인 / redirect / sub-job 모두 v0.4+ 보류

신규 SC (Hawkeye 작성 예정):
- merge_into / supersede / delete 각각 정상 실행 + log "executed:" 라인
- WIKI_COMMAND job 흐름 (자연어 → plan → 실행)
- Wiki Command 탭 UI 동작
- create 충돌 강등의 Phase B 정상 실행
- `[[삭제된페이지]]` 클릭 시 SC-39 안내 페이지 자연 처리 (회귀)

## 회고

(완료 시 작성)
