# v0.3.1 — Phase B: Plan 액션 실행 + Wiki Command

## 기간 / 상태

2026-05-09 ~ ? / **In Progress** (Kirin 승인 2026-05-09)

## 목표

[ADR-0011 Phase A](../decisions/0011-staged-llm-pipeline.md)에서 skip + log였던 `merge_into`/`supersede`를 정상 실행으로 전환하고, 사용자가 자연어로 다중 페이지 작업(분리/통합/삭제)을 요청할 수 있는 **Wiki Command** 진입점을 도입한다. 페이지 삭제 라이프사이클도 함께 도입.

## 스코프

**포함:**
- [ADR-0012 Phase B](../decisions/0012-phase-b-actions-and-wiki-command.md) 전체:
  - Plan 액션 `merge_into`/`supersede` 정상 실행
  - `create` 충돌 자동 강등의 Phase B 정상 실행
  - 신규 Plan 액션 `delete` (페이지 삭제)
  - 신규 Job 타입 `WIKI_COMMAND` + 자연어 명령 처리
  - Wiki Command UI (별도 탭/페이지)
  - log.md `executed:` 라인 형식 추가
  - 단일 job + 다중 액션 구조 유지 (현재 Phase A 구조 그대로)

**제외 (v0.4+):**
- 사용자 승인 절차 (Plan Review 화면) — 웹소켓/이슈트래킹 수준 복잡도
- redirect 기능 — 데이터 모델 단순 유지
- Sub-job 모델 — 운영 필요성 발생 시
- Lint를 통한 삭제 정리 — B-6 (별도)

## 태스크 (셋이 검토 후 확정)

| ID | 제목 | 담당 | 상태 | 추정 |
|----|------|------|------|------|
| T-3.1-0 | ADR-0012 + 신규 SC 작성/검토 (SC-52~62) | Hawkeye | In Progress | — |
| T-3.1-1 | `DeleteAction` Pydantic 모델 + Plan 액션 분기 + INGEST에서 delete 거부 | Breda | Todo | 4-6분 |
| T-3.1-2 | `merge_into` 실행 (Plan 출력 그대로 적용, frontmatter union) | Breda | Todo | 4-6분 |
| T-3.1-3 | `supersede` 실행 (new_content 그대로 대체) | Breda | Todo | 3-5분 |
| T-3.1-4 | `delete` 실행 (WIKI_COMMAND 전용 + 예약 파일 보호) | Breda | Todo | 5-7분 |
| T-3.1-5 | `create` 충돌 자동 강등의 정상 실행 (Phase A skip 제거) | Breda | Todo | 3-5분 |
| T-3.1-6 | 신규 Job 타입 `WIKI_COMMAND` + `POST /api/wiki/commands` (Member/Admin) | Breda | Todo | 7-10분 |
| T-3.1-7 | `prompts/wiki_command.txt` 작성 (분리/통합/삭제 예시) | Breda | Todo | 8-12분 |
| T-3.1-8 | Wiki Command UI (`/command` + nav, 제출 후 Jobs 탭으로 이동) | Breda | Todo | 7-10분 |
| T-3.1-9 | 통합 검증 + 회귀 테스트 (SC-52~62 + 기존 회귀) | Hawkeye | Todo | 7-12분 |

**구현 합계 추정**: 41-61분
**Wall-clock 추정**: best ~50분 / realistic 65-80분 / buffer ~95분

## 변경/결정

ADR-0012 Accepted (Kirin 결정 2026-05-09):
- merge_into/supersede 실행은 **Plan 출력 그대로** (옵션 a). 재호출 X. 통합 품질 부족 시 v0.4+에서 옵션 b 검토.
- delete는 **WIKI_COMMAND 전용**. INGEST plan에서 delete 등장 시 거부.
- WIKI_COMMAND는 **Member도 가능** (Edit과 일관).

셋이 합의:
- merge_into frontmatter: `created` 보존 / `last_updated` 갱신 / `tags` union / `type` 기존 / `sources` union (INGEST일 때만 강제 주입)
- 빈 plan → SUCCESS + log "no actions"
- 받은 순서, 액션별 독립 실행, 단일 commit
- `/command` 별도 페이지, 제출 후 Jobs 탭으로 이동
- log 라인 prefix 통일 (`executed:` / `rejected:` / `skipped:`)

신규 SC (Hawkeye 작성):
- SC-52~56 Phase B 액션 실행 + 안전장치
- SC-57~60 WIKI_COMMAND 흐름 + UI (Member/Admin)
- SC-61 log 라인 prefix 통일
- SC-62 [Advisory] 삭제 후 wikilink 회귀
- (선택) SC-63 INGEST plan delete 거부 검증 (Hawkeye 판단)

## 회고

(완료 시 작성)
