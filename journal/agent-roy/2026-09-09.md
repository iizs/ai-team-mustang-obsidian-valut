---
date: 2026-09-09
agent: roy
topics: [dooray-skill v0.1 완료 · Track A 툴 도입 · 실증, dooray-skill v0.2 · task CRUD · workflow · tag · log 커버]
tags: [policy, tooling, dooray, track-a]
---

## dooray-skill v0.1 완료 · Track A 툴 도입 · 실증

[[projects/dooray-skill/README]] 착수부터 v0.1 실증까지 한 세션에 완료 — 인증 + `me` + `projects.list` + `tasks.list` 를 Sandbox 프로젝트에서 정상 응답. API base URL 정정(`api.dooray.com`), `assignee=me` 자동 해석 등 결정 4건이 프로젝트 문서 결정 기록에 추가됨. Kirin이 `iizs/dooray-skill` (private) 리포로 관리.

[[projects/team-operations-rework/README]] Track A(협업 인프라) 의 툴 선정·최소 통합 마일스톤이 이 지점에서 사실상 달성. 다음은 Breda/Hawkeye 봇 유저 신설 + 위크플로우 · 태그 · 댓글 등 v0.2 기능 확장.

## dooray-skill v0.2 · task CRUD · workflow · tag · log 커버

[[projects/dooray-skill/README]] v0.2 완료로 상태 갱신. Kirin이 지정한 범위(task 조회 필터, workflow 관리, tag 관리, task CRUD + actions, 댓글 관리)를 전부 API 배선. 4개 신규 모듈(workflows/tags/logs/members) + tasks.py 확장. 총 CLI 서브커맨드 20+개. Sandbox 프로젝트에서 create → set-workflow → log-create → log-update → set-done → log-delete까지 e2e 실증 성공. 파일 첨부(multipart)만 v0.3+로 유예.

**남은 결정**: SKILL.md를 `~/.claude/skills/dooray/` 로 실제 배치할지 (심볼릭 링크 vs 복사 vs 각 세션에서 필요할 때 로드).
