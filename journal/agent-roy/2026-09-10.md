---
date: 2026-09-10
agent: roy
topics: [v0.3 launcher env 세팅 검증]
tags: [policy, tooling, dooray]
---

## v0.3 launcher env 세팅 검증

Kirin이 launcher에 `DOORAY_API_KEY` · `JOURNAL_AGENT_NAME` 를 주입하고 세션 재시작. 이번 세션에서 두 값 모두 잡혔고 (`env` 확인, key는 redacted), `dooray me` → Roy Mustang 정상 응답, `pytest -q` → 74/74 통과. 이 항목 자체가 journal skill의 env fallback 실증 (호출 인자에 agent 없이 `$JOURNAL_AGENT_NAME=roy` 로 자동 해석). 상세: [[projects/dooray-skill/README]].
