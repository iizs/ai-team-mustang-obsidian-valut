---
date: 2026-09-22
agent: roy
topics: [discord-agent-runner v0.1 완료 (agent-envy e2e 검증), Discord interactive 폐기 · team launcher 스크립트 정비]
tags: [implementation, milestone, infra, cleanup]
---

## discord-agent-runner v0.1 완료 (agent-envy e2e 검증)

[[projects/discord-agent-runner/README]] 정본. Per-agent Discord ↔ Claude headless 브릿지 · 채널당 session_id 유지 (30분 유휴 리셋 · `!reset`) · per-channel FIFO 큐 · 2000자 chunk 분할. ai-teams 리포 안 (별 repo 없음). 스크립트: `scripts/discord-agent-runner-{start,stop}.sh`. Envy 봇으로 실 Discord 왕복 검증 완료 — 첫 메시지 새 session, 4분 후 후속 메시지 같은 session_id 재개 확인. Gotcha: `load_dotenv(override=False)` 였을 때 launcher shell 의 `DISCORD_BOT_TOKEN` 이 .env 값을 가려 봇 identity 오염 → `override=True` 로 fix (agent-<name>/.env 가 정본이라는 원칙 재확인). [[projects/team-operations-rework/README]] A7 방향 "완전 폐기" → "Discord 만 쓸 에이전트 옵션 유지" 로 조정.

**남은 결정**: 부팅 자동화 (launchd) · 로그 rotation · `plugin:discord@inline` 완전 제거 시점.
**관련**: ai-teams `d5f547b`, `3be94a4` · vault `6382349`.

## Discord interactive 폐기 · team launcher 스크립트 정비

[[projects/team-operations-rework/README]] A7 완료. `scripts/start.sh` 에서 `plugin:discord@inline` + `--dangerously-load-development-channels` 제거 (ai-teams `8d0c02d`) — 재시작 후 매번 뜨던 확인 프롬프트 · discord 플러그인 로드 사라짐 · mustang-hub-agent 플러그인 monitor 는 그대로 유지 (WS `ws_connections: {roy: 1}` 재접속 확인). Discord 필요한 에이전트(envy·lust)는 [[projects/discord-agent-runner/README]] 브릿지로 이관 완료. Team launcher 스크립트 두 개(`start-team-mustang.sh` `adf824d`+`b015714`, `start-team-homunculus.sh` `9b636ea`+`adf824d`) 는 `send-keys` → `new-session`/`split-window` 마지막 인자로 명령 직접 전달 + `; exec zsh` idiom 으로 리팩터. ZLE 렌더링과 send-keys 도착 경합으로 생기던 문자 잘림·이중 echo 해결.

**관련**: [[projects/team-operations-rework/README]] A7 완료 반영 (vault `bd5d24d`).
