---
date: 2026-09-15
agent: roy
topics: [mustang-agent-plugin 설계 초안 · lifecycle · 우선순위 규칙, Plugin monitor 스키마 실증 · Path Y 확정 · human 필터 추가]
tags: [architecture, plugin, design, decisions, implementation]
---

## mustang-agent-plugin 설계 초안 · lifecycle · 우선순위 규칙

[[projects/mustang-agent-plugin/README]] 신규 설계 초안. Plugin monitor 공식 문서 확인(experimental, `command`+env 확장 지원, `ws:` source 지원 여부 미명시)으로 실증 대기 지점 명확화. 스코프 얇게 확정 — Monitor auto-start만, queue 도구 기각(Dooray = truth). 세션-측 `task-hub-loop` skill이 lifecycle 담당: drain(actionable Dooray task 처리) → monitor(WS 대기) → triggered re-check(notification or `/loop` wake-up). 우선순위 3축 규칙 정의 (overdue > priority > 생성시각 asc). [[projects/team-operations-rework/README]] Track A4/A5 링크 갱신, 결정 기록 3건 추가 (v0.2 완료 · identity 규약 · 플러그인 설계 초안).

**남은 결정 (Kirin 리뷰 대기)**: Plugin `ws:` 지원 실증 (Path X vs Y) · skill 위치 (플러그인 내부 확정 후보) · CLAUDE.md에서 skill 강제 호출 명시 수준 · 실패 시 escalation 정책.

## Plugin monitor 스키마 실증 · Path Y 확정 · human 필터 추가

Plugin monitor 매니페스트 실증 완료 (`--debug-file`로 zod validation 에러 확인). `ws:` 는 unrecognized_keys, `command` 만 인식 → **Path Y 확정**. WS consumer 스크립트 프로토타입 실증 성공 (수신 → stdout 한 줄 요약). 실패 시 monitor 만 조용히 drop, 플러그인 나머지 요소는 로드 정상. [[projects/mustang-agent-plugin/README]] 결정 기록 3건 · 미결 3건으로 갱신.

**Human 필터** 요구사항 반영: `mustang-task-hub c9e587f`에 `TASK_HUB_HUMAN_AGENTS` env 추가. `kirin` 을 사람으로 지정, Roy → Kirin 태스크 생성 → target=kirin(human) → skip 확인 (`dropped_human=1`, `route.human_skipped` 로그). Dooray 자체 알림에 위임.

**남은 결정**: task-hub-loop skill 위치 · CLAUDE.md 트리거 명시 수준 · 실패 코멘트 표준.
