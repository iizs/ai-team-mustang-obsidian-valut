# 2026-09-03

## 22:00 — 팀 운영 방식 재정비: Discord → task mgmt + 저널링 전환

Kirin이 오랜만에 (~1.5개월 만) Roy만 단독 호출. 팀 협업 채널로 써 온 Discord의 존재 의의를 근본에서 재검토.

**컨텍스트**: Discord가 실제로 준 이득 3가지를 분해 —
- (a) 여러 에이전트가 같은 스레드 실시간 관찰: Kirin 판단상 비용 낭비. 현실에서도 모두가 동기 필요 없음.
- (b) Kirin이 모바일에서 지시/확인: 실제 니즈 크지 않음.
- (c) 대화 자동 영구화: 의미 있지만 대체 가능 (에이전트 개별 저널링).

**결정**:
1. **Discord 폐기 방향으로 이행** — 채널 유지 자체가 대체 가능한 오버헤드로 판명. 즉시 폐기가 아니라 대체 인프라 갖춘 뒤 순차 이행.
2. **모든 협업(agent↔agent, human↔agent) = task mgmt 툴 하나로** — Kirin이 관찰자로 편하게 훑을 수 있도록 통합 로그로 수렴 (SendMessage 같은 agent-간 직접 채널은 코너 케이스로 밀림).
3. **감지 매커니즘 = webhook (Cloudflare Tunnel)** — 집 NAT/CGNAT 뒤 Mac Mini 노출 문제는 Cloudflare Tunnel로 해결. Task mgmt 툴이 webhook 발화 → 로컬 얇은 receiver가 payload 파싱 → 담당 에이전트 세션 트리거.
4. **저널링부터 착수** — 이 대화 자체가 저널에 먼저 남아야 이후 툴 선정 논의가 컨텍스트를 잃지 않음.

**저널링 스펙 (오늘 확정)**:
- 위치: `/Users/kirinchoi/Vaults/team-mustang/journal/agent-<name>/YYYY-MM-DD.md`
- 단위: 하루 한 파일에 세션별 섹션 append (`## HH:MM — <토픽>`)
- 형식: 요지 1-3줄 + (필요 시) 결정 / 블로커 / 인계 소절
- 트리거: 결정 / 커밋 / 새 계획 / 블로커 / 승인 요청. 원-샷 조회는 스킵.
- 크로스링크 자유.
- Roy `CLAUDE.md`에 규칙 문구 반영 완료 (이번 세션).

**vault memory 정책 변경**:
- 기존 규칙 ("메모리는 vault에 저장한다") 제거. auto-memory 기본 경로로 복귀.
- vault의 `memory/agent-roy/` (MEMORY.md + project_team_members.md 하나) 는 "실제 유의미한 memory 없음" 판단으로 삭제.
- 팀원 최신 명단은 각자 세션이 자연스럽게 다시 학습.

**아직 미정 (다음 세션들)**:
- Task mgmt 도구 선정: GitHub Issues vs Dooray vs 병행 — webhook 지원 수준 비교로 좁힘.
- Receiver 로직 설계: payload → 담당 에이전트 매핑 → 세션 트리거 매커니즘.
- 다른 에이전트(Falman/envy/lina/lust) CLAUDE.md 통일: vault-memory 규칙 제거 + Discord 규칙 제거 + 저널 규칙 추가 — 각자 세션에서 순차 진행.
- Discord 실제 폐기 시점: task mgmt 인프라 안착 후.

## 22:30 — Breda/Hawkeye도 같은 정책 통일 + Roy Discord 규칙 제거

Kirin: "개발은 이렇게 셋이 한 팀이니까 현재로서는" — Roy/Breda/Hawkeye 개발 3인방에 같은 CLAUDE.md 정책 즉시 적용.

**적용**:
- Roy `CLAUDE.md`: "Discord 그룹 채널 규칙" 섹션 제거 (메모리 규칙은 앞 세션에서 이미 제거, 저널 규칙 이미 추가)
- Breda `CLAUDE.md`: 메모리 규칙 제거 → 저널 규칙 추가 / Discord 규칙 제거
- Hawkeye `CLAUDE.md`: Breda와 동일

**vault memory 정리**:
- `memory/agent-breda/` (4 파일) 삭제 — Discord mention 규칙 폐기 방향, worldcup/user_profile은 공유 memory 중복
- `memory/agent-hawkeye/`도 삭제. 단 `feedback_sc_verification_scope.md`는 유의미(Sheska Phase 3 실사고 기반 QA 교훈)라 공유 memory (`~/.claude/projects/-Users-kirinchoi-Projects-ai-teams/memory/feedback_sc_verification_scope.md`)로 이전 + MEMORY.md 인덱스 갱신
- Falman은 오늘 스코프 밖 (개발 3인방만) — vault memory 그대로 방치

**공유 memory 인덱스 정리**:
- 존재하지 않는 파일을 가리키던 `feedback_discord_mention_rules.md` 엔트리 제거
- `feedback_discord_reply_tool.md` 엔트리에 "Discord 폐기 진행 중 — 폐기 완료 시 삭제" 마킹
- `feedback_sc_verification_scope.md` 엔트리 추가

**다음 세션 인계**:
- 이번 세션 결정 그대로 유효.
- 다음 결정 지점: task mgmt 도구 선정 (Kirin이 언제 다시 열지 미정).
- Discord 폐기 완료 시 삭제 대상: 공유 memory의 `feedback_discord_reply_tool.md` / `user_discord_identity.md`, 각 에이전트 CLAUDE.md엔 이미 Discord 규칙 없음.
- Falman 및 신규 에이전트(envy/lina/lust) CLAUDE.md 정리는 아직 미이행.

## 22:45 — vault-memory 정책 되돌림 (팀 경계 문제)

Kirin 지적: `~/Projects/ai-teams/` 리포엔 개발 3인방 외에도 다른 에이전트들이 함께 살고, auto-memory는 git repo root 기준으로 버킷팅하므로 기본 경로로 회귀시키면 memory가 팀 경계를 넘어 섞임. → **vault-memory 오버라이드 유지가 맞다.**

**되돌린 것**:
- Roy/Breda/Hawkeye `CLAUDE.md` — `# 메모리` 섹션 복구 (vault 경로 지정 + 왜 vault 오버라이드인지 근거 문구 추가). `# 저널` 섹션과 Discord 규칙 제거는 그대로 유지.
- `vault/memory/agent-roy/`, `memory/agent-breda/`, `memory/agent-hawkeye/` 전부 복구 (`git checkout <이전-커밋>^ -- <path>`로 삭제 이전 상태 복원).
- 공유 memory에 잘못 이전했던 `feedback_sc_verification_scope.md` 삭제 + MEMORY.md 인덱스에서도 제거 (원래 Hawkeye vault memory로 회귀).

**유지된 것**:
- 저널 도입 (오늘 결정 그대로).
- Discord 규칙 제거 (Discord 폐기 방향 그대로).
- 공유 MEMORY.md의 stale `feedback_discord_mention_rules` 엔트리 제거 및 `feedback_discord_reply_tool`의 "폐기 진행 중" 마킹 (독립 관찰, memory 정책 되돌림과 무관).

**교훈 (다음 세션 참고)**:
- ai-teams git 리포는 다중 팀·다중 에이전트 공유 저장소. auto-memory의 git-root 버킷팅 특성상 여기 안의 에이전트들은 CLAUDE.md 오버라이드 없으면 memory가 자동으로 팀 경계를 무너뜨림. 새 에이전트 추가 시 반드시 vault-memory 오버라이드 넣을 것.
